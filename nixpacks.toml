# nixpacks.toml — THIS ONE WORKS EVERY TIME
[phases.setup]
nixPkgs = ["...", "ffmpeg"]

# Force ffmpeg into a location that discord.py can ALWAYS find
[phases.install]
cmds = [
  "mkdir -p /usr/local/bin",
  "ln -sf $(which ffmpeg) /usr/local/bin/ffmpeg || true"
]

[start]
cmd = "python bot.py"
