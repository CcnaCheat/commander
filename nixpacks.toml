# nixpacks.toml
[phases.setup]
  nixPkgs = ["...", "ffmpeg", "opusTools", "libopus"]

# Optional: make sure python has audio support
[phases.install]
  cmds = ["pip install -r requirements.txt discord.py[voice]"]

[start]
  cmd = "python bot.py"
